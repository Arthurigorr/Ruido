% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgNoise.R
\name{bgNoise}
\alias{bgNoise}
\title{Background Noise and Soundscape Power Index}
\usage{
bgNoise(
  soundfile,
  channel = "stereo",
  timeBin = 60,
  dbThreshold = -90,
  targetSampRate = NULL,
  wl = 512,
  window = signal::hamming(wl),
  overlap = ceiling(length(window)/2),
  histbreaks = "FD"
)
}
\arguments{
\item{soundfile}{A tuneR Wave object or the path to a valid audio in your computer}

\item{channel}{The channel you want to computer of your soundfile. Available channels are: "stereo", "mono", "left" or "right"}

\item{timeBin}{The size (in seconds) of the time bin (default = 60)}

\item{dbThreshold}{The minimum possible value of dB for the spectrograms (default = -90)}

\item{targetSampRate}{The sampling rate of your audio (this argument is only used to down sample your audio)}

\item{wl}{The window length of your spectrogram (default = 512)}

\item{window}{The window used to smooth the signal (default = hamming(wl))}

\item{overlap}{Overlap between the spectrogram windows (the default is half your window length)}

\item{histbreaks}{Which breaks to use to calculate background noise. Available breaks are: "FD", "Sturges", "scott" and 100}
}
\value{
A list containing three objects: The first and second one contains a matrix with the values of background noise and soundscape power respectively to each time bin and for each frequency window of your soundfile. The third object is the duration in second of your time bins.
}
\description{
Compute the Background Noise and Soundscape Power values of an audio using Towsey 2017 methodology
}
\details{
Background noise is an index that measures the most common continuous baseline level of acoustic energy in a frequency window and in a time bin. It is calculated by taking the modal value of intensity values in temporal bin c in frequency window f:

\deqn{BGN_{f} = mode(dB_{cf})}

Soundscape power represents a measure of signal-to-noise ratio. It measures the relation of background noise to the loudest intensities in temporal bin c in frequency window f:

\deqn{POW_{f} = max(dB_{cf}) - BGN_{cf}}
}
\examples{
### For our main example we'll create an artificial audio with white noise to test its Background Noise
# We'll use the package tuneR
library(tuneR)

# Define the audio sample rate, duration and number of samples
sampRate <- 12050
dur <- 59
sampN <- sampRate * dur

# Then we Ggenerate the white noise for our audio and apply FFT
set.seed(413)
ruido <- rnorm(sampN)
spec <- fft(ruido)

# Now we create a random spectral envelope for the audio and apply the spectral envelope
nPoints <- 50
env <- runif(nPoints)
env <- approx(env, n=nPoints)$y
specMod <- spec * env

# Now we invert the FFT back to into a time waveform and normalize and convert to Wave
out <- Re(fft(specMod, inverse=TRUE)) / sampN
wave <- Wave(left = out, samp.rate = sampRate, bit = 16)
wave <- normalize(wave, unit = "16")

# Here's our artificial audio
wave

# Running the bgNoise function with all the default arguments
bgn <- bgNoise(wave)

# Print the results
head(bgn$left$BGN)
head(bgn$left$POW)

# Plotting background noise and soundscape profile for the first minute of the recording
par(mfrow = c(2,2))
plot(x = bgn$left$BGN$BGN1, y = seq(1,bgn$sampRate, length.out = 256),
     xlab = "Background Noise (dB)", ylab = "Frequency (hz)",
     main = "BGN by Frequency",
     type = "l")
plot(x = bgn$left$POW$POW1, y = seq(1,bgn$sampRate, length.out = 256),
     xlab = "Soundscape Power (dB)", ylab = "Frequency (hz)",
     main = "POW by Frequency",
     type = "l")
plot(bgn$left$BGN$BGN1~bgn$left$POW$POW1, pch = 16,
     xlab = "Soundscape Power (dB)", ylab = "Background Noise (dB)",
     main = "BGN~POW in left ear")
hist(bgn$left$BGN$BGN1, main = "Histogram of BGN distribution",
      xlab = "Background Noise (BGN)")

\dontrun{
  ### This is a secondary example using audio from a real soundscape
  ### These audios are originated from the Escutadô Project
  # Getting audiofile from the online Zenodo library
  dir <- tempdir()
  rec <- paste0("GAL24576_20250401_", sprintf("\%06d", 0),".wav")
  recDir <- paste(dir,rec , sep = "/")
  url <- paste0("https://zenodo.org/records/17575795/files/", rec, "?download=1")

  # Downloading the file, might take some time denpending on your internet
  download.file(url, destfile = recDir, mode = "wb")

  # Running the bgNoise function with all the default arguments
  bgn <- bgNoise(recDir)

  # Print the results
  head(bgn$left$BGN)
  head(bgn$left$POW)

  # Plotting background noise and soundscape profile for the first minute of the recording
  par(mfrow = c(2,2))
  plot(x = bgn$left$BGN$BGN1, y = seq(1,bgn$sampRate, length.out = 256),
       xlab = "Background Noise (dB)", ylab = "Frequency (hz)",
       main = "BGN by Frequency",
       type = "l")
  plot(x = bgn$left$POW$POW1, y = seq(1,bgn$sampRate, length.out = 256),
       xlab = "Soundscape Power (dB)", ylab = "Frequency (hz)",
       main = "POW by Frequency",
       type = "l")
  plot(bgn$left$BGN$BGN1~bgn$left$POW$POW1, pch = 16,
       xlab = "Soundscape Power (dB)", ylab = "Background Noise (dB)",
       main = "BGN~POW in left ear")
  plot(bgn$right$BGN$BGN1~bgn$right$POW$POW1, pch = 16,
       xlab = "Soundscape Power (dB)", ylab = "Background Noise (dB)",
       main = "BGN~POW in right ear")

  unlink(recDir)
}
}
\references{
Towsey, Michael W. (2017) The calculation of acoustic indices derived from long-duration recordings of the natural environment.

Pijanowski, B. C. (2024). Principles of Soundscape Ecology.
}
